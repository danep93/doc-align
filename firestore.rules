rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client access. All Firestore access goes through the backend
    // API which uses Firebase Admin SDK (bypasses these rules).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
